[package]
name = "cutlass-compositor"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cutlass frame compositor"

[dependencies]
cutlass-decode = { path = "../cutlass-decode" }
tracing.workspace = true
