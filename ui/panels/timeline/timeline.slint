import { HorizontalDivider } from "../../components/divider.slint";
export component TimelinePanel inherits VerticalLayout {
    spacing: 0;

    Rectangle {
        height: 40px;

        Text {
            text: "Tools";
            color: #ffffff;
        }

        HorizontalDivider {
            background: #000000;
            y: parent.height - self.height;
        }
    }
}
