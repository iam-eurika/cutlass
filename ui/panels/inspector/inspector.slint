import { PanelBase } from "../../components/index.slint";
import { AppStore } from "../../store/index.slint";

export component InspectorPanel inherits PanelBase {
    VerticalLayout {
        spacing: AppStore.theme.metrics.spacing.none;

        Rectangle {
            vertical-stretch: 1;
            horizontal-stretch: 1;
        }
    }
}
