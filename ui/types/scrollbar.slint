export struct Scrollbar {
    size: length,
    thumb-inset: length,
    thumb-min: length,
    thumb-radius: length,
    track: color,
    thumb: color,
    thumb-hover: color,
    thumb-active: color,
}
